const path = require("path");

const db_path = "./";

module.exports = {
  config: path.resolve(db_path, "src/config/config.cjs"),
  "models-path": path.resolve(db_path, "src/models"),
  "seeders-path": path.resolve(db_path, "src/seeders"),
  "migrations-path": path.resolve(db_path, "src/migrations"),
};
